[package]
name = "contracts"
version = "0.1.0"
edition.workspace = true
build = "build.rs"

[dependencies]
prost = "0.12"
prost-types = "0.12"
tonic = { version = "0.11", features = ["transport"] }

[build-dependencies]
tonic-build = { version = "0.11", features = ["prost"] }
